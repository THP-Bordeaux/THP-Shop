<h1>Index items</h1>
<% if  @items.empty? %>
  <%= "Pas d items pour le moment faites rails db:seed "%>
<% else

  @items.each do |item|%>
    <p>
      <%=link_to "title = "+ item.product_title, item%>
    </p>
  <% end

end %>
